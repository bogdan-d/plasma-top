__pycache__/
*.pyc
*.pyo

rust/target/
# Per-lane isolated target dirs (avoids collisions when multiple lanes build
# in parallel); see plans/LANES.md.
rust/target-*/

# Tooling caches / agent config (not part of the shipped tool)
.claude/
.ruff_cache/
.pytest_cache/

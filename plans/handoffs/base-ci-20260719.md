# Handoff: `BASE` / `base-ci-20260719`

## Contract

- Objective: Implement the smallest truthful Phase 0 CI slice only: add a GitHub Actions workflow that verifies the already-established Python baseline checks on `push` and `pull_request`, without introducing any Rust CI.
- Integration base SHA: `bff3f292b0369cf9a871c259384477b3a186a6d4`
- Branch/worktree: `rust-migration-base-bootstrap` @ `/var/mnt/xdata/code/_self/plasma-stats`
- Owned paths:
  - `.github/workflows/baseline.yml`
  - `plans/handoffs/base-ci-20260719.md`
- Forbidden/shared paths:
  - `src/**`
  - `tests/**`
  - `config/**`
  - `style/**`
  - `plans/STATUS.md`
  - `plans/LANES.md`
  - `plans/INVENTORY.md`
  - `README.md`
  - `AGENTS.md`
  - `packaging/**`
  - `install.sh`
  - `uninstall.sh`
  - `.gitignore` files
- Dependencies verified integrated:
  - pre-existing `requirements-dev.txt`
  - pre-existing repo pytest suite under `tests/`
  - existing `tests/test_lint.py` and `tests/test_deadcode.py`, which remain transitively exercised via `python3 -m pytest tests/ -v`

## Result

- Status: `handoff`
- Commits: none (working tree changes only)
- Changed files:
  - `.github/workflows/baseline.yml` — minimal GitHub Actions workflow for the existing Python baseline on `push` and `pull_request`
  - `plans/handoffs/base-ci-20260719.md` — this handoff
- Behavior implemented/preserved:
  - workflow triggers on both `push` and `pull_request`
  - runner is `ubuntu-latest`
  - job checks out the repository, installs Python `3.11`, installs `requirements-dev.txt`, and runs `python3 -m pytest tests/ -v`
  - lint and dead-code checks remain owned by the existing pytest suite; no duplicate standalone `ruff` or `vulture` CI steps were added
  - no Rust toolchain, Cargo job, matrix, cache, or artifact upload was introduced
- Explicitly not implemented:
  - no Rust CI or toolchain bootstrap
  - no branch/path filters
  - no separate explicit lint/dead-code jobs outside pytest
  - no modifications to plans/status/inventory docs beyond this handoff

## Parity evidence

- Current Python symbols/files covered:
  - `requirements-dev.txt`
  - `tests/test_lint.py`
  - `tests/test_deadcode.py`
  - the existing `tests/` pytest entrypoint already used for the verified local baseline
- Oracle fixtures/cases: none (CI wiring only)
- Exact differences remaining:
  - local validation cannot execute a GitHub-hosted runner, so the first remote `push` or `pull_request` should confirm the hosted Ubuntu environment resolves the existing Python dependencies as expected
  - this host did not have a YAML parser module available for an extra machine parse, so YAML verification here was limited to manual inspection plus patch hygiene checks
- Inventory entries proposed resolved: none; this slice closes a CI gap, not an inventory ledger gap

## Validation

| Command | Result | Notes/artifact |
|---|---|---|
| `manual inspection of .github/workflows/baseline.yml` | pass | Confirmed only the requested trigger/job shape: `push`, `pull_request`, Python `3.11`, `requirements-dev.txt`, and `python3 -m pytest tests/ -v`; no Rust or duplicated lint/dead-code jobs. |
| `git diff --check` | pass | No whitespace or patch-format issues in the scoped changes. |
| `git status --short --untracked-files=all` | pass | Final workspace delta is limited to `.github/workflows/baseline.yml` and `plans/handoffs/base-ci-20260719.md`. |
| `python3 - <<'PY' ...` | pass | Readback smoke confirmed the workflow file is present (`workflow_lines=23`); local host reported `yaml_parser=missing`, so no parser-backed YAML load was available without adding tooling. |

## Dependencies and safety

- New/changed dependencies and review:
  - no repo dependency changes; CI installs only the pre-existing `requirements-dev.txt`
  - external GitHub Actions used: `actions/checkout@v6` and `actions/setup-python@v6`
- Native/build/runtime requirements:
  - targets GitHub-hosted `ubuntu-latest`
  - requires only Python `3.11` plus whatever `requirements-dev.txt` already resolves today
- Unsafe/FFI locations and invariants:
  - none touched

## Risks/blockers

- Known risks:
  - hosted-runner behavior is inferred from current GitHub Actions docs and local baseline success, but not yet proven in this workspace until the workflow runs remotely
  - if `requirements-dev.txt` grows platform-specific assumptions later, this single Ubuntu job may need follow-up adjustments
- Blocker requiring integration decision:
  - none for this slice
- Suggested next lane/API change:
  - after merge, watch the first GitHub Actions run and only then decide whether Phase 0 needs additional niceties such as caching, explicit version pinning, or future Rust jobs

## Review notes

- Diff inspected for out-of-scope paths: yes
- Production runtime untouched by tests: yes
- No skipped/weakened checks: yes
- Rebase required before merge: no

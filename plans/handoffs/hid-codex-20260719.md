# Handoff: `HID` / `hid-codex-20260719`

## Contract

- Objective: port `src/bolt_battery.py` receiver discovery and HID++ 2.0
  device-name/battery query protocol, then implement POWER's frozen
  `BoltBatteryFacade`.
- Integration base SHA: `c2921cd`.
- Branch/worktree: `rust-migration-base-bootstrap` at repository root.
- Owned paths: `rust/src/sensors/hid.rs` and focused inline tests.
- Integration-owned paths touched: `rust/src/sensors/mod.rs` registration;
  `domain/boundary.rs` typed HID error; POWER facade documentation;
  `Cargo.toml`/`DEPENDENCIES.md` existing `nix` feature/review; plan ledgers.
- Dependencies verified: SCAFFOLD, FIXTURES, and POWER are verified. POWER's
  `BoltBattery`/`BoltBatteryFacade` contract was consumed directly.

## Result

- Status: `verified` after integration review.
- Implementation commit: `5b38850` (`Port Rust Bolt HID battery protocol`).
- Changed implementation:
  - `rust/src/sensors/hid.rs`: sorted sysfs discovery for Bolt PID `c548` on
    interface `.2`; direct hidraw open/write/read; 1-second poll timeout;
    ten-response match bound; ROOT feature lookup; DEVICE_NAME and
    UNIFIED_BATTERY requests; Python-compatible ASCII replacement/trim.
  - `rust/src/domain/boundary.rs`: `HidFailed` with optional device path.
  - `rust/src/sensors/{mod,power}.rs`: module registration and exact no-level
    versus hard-I/O facade contract documentation.
  - `rust/Cargo.toml`, `rust/DEPENDENCIES.md`: enable existing `nix` `poll`
    feature and record rejection of an additional native HID dependency.
- Explicitly not implemented: collector/daemon construction of the facade
  (Phase 5); live Bolt-device validation (Phase 7, unavailable on this host).

## Parity evidence

- Python symbols resolved: `_load_hidapi`, `_bolt_hidraw`, `_xfer`,
  `_get_feature_idx`, `_get_battery`, `_get_name`, and `query`.
- `_load_hidapi` is intentionally replaced, not reproduced: direct hidraw file
  I/O removes ctypes/FFI and import-time `libhidapi` loading while retaining the
  same device and report protocol.
- An isolated Python oracle import with fake ctypes library and `_xfer` captured
  exact packet bytes. Rust's fixed assertions match:
  - ROOT battery feature: `110400011004` + 14 zero bytes.
  - Battery function: `11040711` + 16 zero bytes.
  - ROOT name feature: `110400010005` + 14 zero bytes.
  - Name function: `1104071100` + 15 zero bytes.
- No exact differences remain in deterministic behavior. No-level outcomes
  (unsupported feature, timeout, short/mismatch) map to `Ok(None)` as Python's
  query does; discovery/open/write failures map to `Err` so POWER retries.
- Inventory entries proposed and accepted: `src/bolt_battery.py`, all seven
  Python symbols, new Rust HID file/callables, and HID boundary error.

## Validation

| Command | Result | Notes |
|---|---|---|
| `cargo test --manifest-path rust/Cargo.toml --all-targets --all-features sensors::hid` | pass | 16 focused tests. |
| `cargo fmt --manifest-path rust/Cargo.toml -- --check` | pass | Clean. |
| `cargo check --manifest-path rust/Cargo.toml --all-targets --all-features` | pass | All targets. |
| `cargo clippy --manifest-path rust/Cargo.toml --all-targets --all-features -- -D warnings` | pass | No warnings. |
| `cargo test --manifest-path rust/Cargo.toml --all-targets --all-features` | pass | 426 library + 23 integration tests. |
| `cargo doc --manifest-path rust/Cargo.toml --no-deps` | pass | No warnings. |
| `.venv/bin/python -m pytest tests/ -q` | pass | 175 passed, 1 optional skip. |
| isolated fake-ctypes Python packet capture | pass | Bytes listed above. |

## Dependencies and safety

- New crates: none. Existing `nix 0.29` gains only its `poll` feature; no new
  transitive package or lockfile change.
- Native/build/runtime additions: none. Rust no longer needs libhidapi for this
  path; normal hidraw permissions remain required.
- Unsafe/FFI: none. Production uses `std::fs`/`std::io` and safe `nix::poll`;
  crate-level `#![deny(unsafe_code)]` remains green.

## Risks/blockers

- Known risk: no supported Bolt receiver/device exists on the current host, so
  real hidraw permissions and hardware timing remain Phase 7 evidence.
- Blocker: none for lane integration.
- Suggested next lane: GPU remains ready; NOTIFY still needs its shared facade.

## Review notes

- Diff inspected for out-of-scope paths: yes; all shared edits are listed.
- Production runtime untouched by tests: yes; sys/dev roots and report I/O are
  fixture/fake-backed.
- No skipped/weakened checks: yes.
- Rebase required: no.

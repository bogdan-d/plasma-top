# Handoff: `GPU` / `gpu-codex-20260719`

## Contract

- Objective: port NVIDIA detection/metrics/fallback/cache plus active-vendor GPU
  graph history from `src/sensors.py`.
- Integration base SHA: `b522135`.
- Branch/worktree: `rust-migration-base-bootstrap` at repository root.
- Owned paths: `rust/src/sensors/gpu_nvidia.rs` and focused inline tests.
- Integration-owned paths touched: sensor module registration, typed `GpuCache`,
  plan ledgers; one narrow POWER test-feature gate correction found by the
  documented no-feature `cargo check --all-targets` command.
- Dependencies verified: PROCESS and all Phase 2/3 foundations are verified.

## Result

- Status: `verified` after local integration review.
- Commit: integration commit containing this handoff.
- Changed implementation:
  - `rust/src/sensors/gpu_nvidia.rs`: NVIDIA PCI display detection; typed
    `NvmlFacade`; permanent init versus retryable read failure; exact
    `nvidia-smi` fallback; 0s NVML/3s fallback cache; 99 clamp; NVIDIA-preferred
    and Intel-fallback graph history.
  - `rust/src/domain/state.rs`: records permanent NVML initialization failure in
    the existing aggregate GPU cache.
  - `rust/src/sensors/mod.rs`: registers the module.
  - `rust/src/sensors/power.rs`: gates fake-dependent tests on `test-support`,
    making the documented no-feature all-target check compile again.
- Explicitly not implemented: production optional-library loading/GPU-0 handle
  construction and full collector wiring (COLLECTOR); live NVIDIA validation
  (Phase 7, unavailable on this AMD-only host).

## Parity evidence

- Python symbols resolved: `_detect_nvidia`, `_sample_gpu_history`,
  `_gpu_cache_ttl`, `_nvidia_cap`, `_read_nvidia_pynvml`, `_read_nvidia_smi`,
  and `_read_nvidia`.
- `_pynvml_handle_get` stays COLLECTOR-owned because it constructs/caches the
  production optional-library handle; `NvmlError::Init` freezes its observable
  once-only failure contract for that lane.
- Fixed Python assertions confirm clamp behavior, fallback tuple order, exact
  timeout kwargs, NVIDIA history cadence, decoder-zero fill, and trimming.
- No differences remain for defined NVIDIA outputs. Malformed byte payloads
  degrade to absent metrics as required by the adapter-failure test matrix.

## Validation

| Command | Result | Notes |
|---|---|---|
| `cargo test --manifest-path rust/Cargo.toml --all-targets --all-features sensors::gpu_nvidia` | pass | 10 focused tests. |
| `cargo fmt --manifest-path rust/Cargo.toml -- --check` | pass | Clean. |
| `cargo check --manifest-path rust/Cargo.toml --all-targets` | pass | No-feature documented gate restored. |
| `cargo clippy --manifest-path rust/Cargo.toml --all-targets --all-features -- -D warnings` | pass | No warnings. |
| `cargo test --manifest-path rust/Cargo.toml --all-targets --all-features` | pass | 436 library + 23 integration tests. |
| `cargo doc --manifest-path rust/Cargo.toml --no-deps` | pass | No warnings. |
| `.venv/bin/python -m pytest tests/ -q` | pass | 175 passed, 1 optional skip. |
| isolated Python GPU oracle assertions | pass | Clamp, fallback order/timeout, history cadence/trim. |

## Dependencies and safety

- New crates/lockfile changes: none. `NvmlFacade` avoids forcing an NVML native
  dependency into deterministic Phase 4 tests.
- Native/build/runtime additions: none in this lane. COLLECTOR must review the
  optional production NVML loader and preserve missing-library fallback.
- Unsafe/FFI: none; crate-level `#![deny(unsafe_code)]` remains green.

## Risks/blockers

- Known risk: no NVIDIA device exists on the current host; driver/library and
  live command behavior remain Phase 7 evidence.
- Blocker: none for GPU integration.
- Suggested next lane: COLLECTOR is now ready; NOTIFY remains facade-blocked.

## Review notes

- Diff inspected for out-of-scope paths: yes; shared edits listed above.
- Production runtime untouched by tests: yes; sysfs, command, and NVML inputs are
  rooted/fake-backed.
- No skipped/weakened checks: yes; POWER cfg correction restores a gate and its
  tests still run under the aggregate all-feature command.
- Rebase required: no.

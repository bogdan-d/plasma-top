# Handoff: `DOMAIN` / `20260719`

## Contract

- Objective: validated tokens, metric metadata, forms, surfaces, placement, capability derivation.
- Integration base SHA: `9a088c2` (Add Phase 1 Rust scaffold with test support and CI validation)
- Branch/worktree: `rust-p2-domain` at `/var/mnt/xdata/code/_self/plasma-stats/.kilo/worktrees/rust-p2-domain`
- Owned paths:
  - `rust/src/domain/registry.rs` (new, 702 lines)
  - `rust/src/domain/mod.rs` (one-line module addition + re-export block — see Cross-lane proposals below)
- Forbidden/shared paths respected:
  - `src/**`, `tests/**` (Python oracle) — read-only, untouched.
  - `rust/src/{lib,main,error,cli}.rs` — untouched.
  - `rust/src/domain/{boundary,form,metric,item}.rs` — frozen P1 contracts, untouched (verified fmt-stable against HEAD).
  - `rust/Cargo.toml`, `rust/Cargo.lock`, `rust/rustfmt.toml`, `rust/DEPENDENCIES.md` — untouched.
  - `plans/{STATUS,INVENTORY,LANES,PHASES,ARCHITECTURE,CONTRACT,TESTING}.md`, `plans/README.md` — untouched.
  - All other lane owned paths (`rust/src/{config,runtime,test_support}/**`) — untouched.
- Dependencies verified integrated:
  - SCAFFOLD contracts at base SHA: `domain::{form,metric,item,boundary}.rs` compile, pass clippy, and pass their P1 tests.
  - Python oracle (`tests/test_items.py`, `tests/test_oracle.py`) — 15/15 passing with the venv at the main repo (`.venv/bin/python`).

## Result

- Status: `handoff`
- Commits: none (worktree clean except for the two intended file changes; integration owner commits after merge).
- Changed files:
  - `rust/src/domain/registry.rs` (new file, 702 lines, 25 tests).
  - `rust/src/domain/mod.rs` (+7 lines: `pub mod registry;` and a `pub use registry::{...};` re-export block). This file is marked frozen by the lane contract — see **Cross-lane proposal** in Risks/blockers.
- Behavior implemented/preserved:
  - **Token boundary** `parse(token: &str) -> Option<(Metric, Option<Form>)>` — wraps `ItemToken::from_str(token).ok()` and maps to Python's `(metric, Option<Form>)` shape. Separators return `None` (matching Python).
  - **Validation** `unknown_item_names(names) -> BTreeSet<String>` — flags tokens that fail to resolve (typos, unsupported forms, form-on-intrinsic-metric), excluding separators.
  - **Validation** `misplaced_items(panel, tooltip) -> (BTreeSet<String>, BTreeSet<String>)` — flags tokens whose effective surfaces don't admit the target surface (panel = either orientation, matching Python `Surface.PANEL = PANEL_H | PANEL_V`).
  - **Capability map** `notification_capability_map() -> &'static [(&'static str, Capability)]` (10 entries) + `graphs_page_capabilities() -> &'static [Capability]` (4 entries).
  - **Capability derivation** `needed_capabilities(items, notify_flags, pages_order) -> BTreeSet<Capability>` — union of metric caps + notification-flag caps + graphs-page caps.
  - **list-items** `list_items() -> Vec<(String, &'static str)>` and `placement_for(SurfaceSet) -> &'static str` — byte-for-byte parity with `pirostats list-items` Python output, exposed for the DAEMON-CLI lane.
  - **Constants** `SEPARATOR_ITEMS`, `NOTIFY_CAPABILITY_MAP`, `GRAPHS_PAGE_CAPABILITIES` — public for downstream lanes (CONFIG/FORMATTER/COLLECTOR).
- Explicitly not implemented:
  - The dispatch half of `src/registry.py` (`_RENDER`, `_historied`, `render`, `render_item`, `item_gate`, `_form_token`) — that belongs to FORMATTER and needs the cells/traces machinery that lands in Wave 3.
  - Hardware gates (`Metric::gate(f, r)`) — Python's gates are property-of-data closures over `f._hw`; the Rust contract defers this to the sensor/collection lanes via `HardwareSnapshot::capabilities` / `Metric::capabilities`. `Metric::gate` is not ported as a function (no `f`/`r` types exist yet); the lane contract did not request it.

## Parity evidence

- Current Python symbols/files covered:
  - `src/registry.py`: `parse`, `unknown_item_names`, `misplaced_items`, `needed_capabilities`, `_NOTIFY_CAPS` (as `NOTIFY_CAPABILITY_MAP`), the graphs-page special case.
  - `src/render_model.py`: `SEPARATOR_ITEMS` (the keys, since the values are CSS class names used only by RENDER-CORE).
  - `src/daemon.py:run_list_items`: enumerated via `list_items()` / `placement_for()` — the 51-row corpus matches the live `pirostats list-items` output captured from the Python oracle.
- Oracle fixtures/cases:
  - **Exhaustive list-items corpus**: 51 entries (26 `panel + tooltip` + 6 `panel only` + 19 `tooltip only`), embedded as `EXPECTED_LIST_ITEMS` constant and asserted row-for-row by `list_items_matches_python_oracle_byte_for_byte`. Ordering is `(placement, token)`, matching Python's `sorted(rows, key=lambda tw: (tw[1], tw[0]))`.
  - **All 14 `tests/test_items.py` tests** ported to Rust:
    - `cpu_usage_needs_no_dedicated_sensor`, `item_pulls_its_capability`, `metric_can_need_multiple_capabilities`, `form_does_not_change_the_capability`, `notification_keeps_sensor_alive_without_the_item`, `unknown_token_contributes_nothing`, `gpu_nvidia_metrics_share_one_capability` (needed_capabilities family).
    - `unknown_item_names_flags_bad_metric_and_bad_form` (plus added intrinsic-with-form case `net_speed:value`).
    - `value_metrics_live_on_both_surfaces`, `bare_visuals_are_panel_only`, `wide_forms_and_string_metrics_are_tooltip_only`.
    - `misplaced_items_flags_tooltip_only_in_panel`, `misplaced_items_flags_panel_only_in_tooltip`, `misplaced_items_ignores_unknown_names` (+ added `misplaced_items_skips_separators`).
  - **Additional parity tests beyond the Python suite**:
    - `parse_round_trips_known_tokens`, `parse_rejects_invalid_and_separator_tokens` — explicit parse boundary documentation.
    - `graphs_page_adds_gpu_and_network_capabilities`, `graphs_page_caps_absent_without_graphs_in_order` — the `pages_order == ["graphs"]` special case (covered in Python only indirectly through `needed_capabilities(cfg)`).
    - `notification_capability_map_covers_python_notify_caps` — asserts the 10-key map and three spelling-mismatch spot-checks (`cpu_temp → CpuTemperature`, `gpu_nvidia_temp → GpuNvidia`, `battery_kbd → BatteryKeyboard`).
    - `separator_set_matches_render_model_keys` — `SEPARATOR_ITEMS == ["separator_small", "separator_big"]`.
    - `intrinsic_tokens_render_without_a_form` — the three intrinsic metrics (`net_speed`, `disk_io`, `top_process`) round-trip with `ItemRendering::Intrinsic(_)` and `form() == None`.
    - `item_token_round_trips_for_every_corpus_entry` — every entry in the corpus survives `Display` round-trip.
    - `misplaced_matrix_matches_effective_surfaces_for_every_token` — exhaustive matrix: for every (metric, form) pair in `Metric::all()` × `MetricSpec::generic_forms` plus the three intrinsic metrics, asserts `misplaced_items` returns the token iff its effective surfaces don't admit the target surface, on both panel and tooltip passes. That is 51 × 2 placement checks.
- Exact differences remaining:
  - **None at the data level.** Every observable value (token set, capability set, placement, validation result) matches the Python oracle byte-for-byte on the corpus.
  - **API shape difference (intentional)**: Python's `needed_capabilities(cfg)` takes a `Config` and folds token parsing into the call. Rust's `needed_capabilities(items, notify_flags, pages_order)` takes primitive iterators so DOMAIN does not depend on CONFIG types. The CONFIG lane will be responsible for flattening its `(panel ∪ tooltip)` token set into `ItemToken`s (filtering parse failures) before calling this function — exactly what the lane contract specifies.
- Inventory entries proposed resolved (from `plans/INVENTORY.md`):
  - `src/registry.py` rows: `_form_token` (deferred to FORMATTER — render half), `parse` (covered), `item_gate` (deferred — needs `f`/`r` types), `needed_capabilities` (covered), `unknown_item_names` (covered), `misplaced_items` (covered). Also `_NOTIFY_CAPS` and the graphs-page cap special case (covered as constants + `needed_capabilities`).
  - `src/forms.py`, `src/metrics.py` — already covered by P1 SCAFFOLD; this lane re-verified parity through the matrix test (`misplaced_matrix_matches_effective_surfaces_for_every_token` walks every metric × every form).
  - `tests/test_items.py` rows 739–754 — every test fn ported; `_cfg` / `_where` helpers mapped to `caps()` / `where_surfaces()` Rust helpers.

## Validation

Toolchain: `cargo 1.93.1`, `rustc 1.93.1`, `rustfmt 1.8.0-stable` (installed via `rust-toolchain.toml`, channel = `stable`). Worktree target dir is `rust/target/` (no collision with other worktrees — verified via `cargo metadata`).

| Command | Result | Notes/artifact |
|---|---|---|
| `cargo fmt --manifest-path rust/Cargo.toml -- --check` | **pass** | exit 0, no diff. Verified `mod.rs` and `registry.rs` are clean; `form.rs`/`item.rs`/`metric.rs`/`boundary.rs` unchanged from HEAD. |
| `cargo check --manifest-path rust/Cargo.toml --all-targets --all-features` | **pass** | `Finished dev profile` in 0.11s; lib + bin targets; `test-support` feature on. |
| `cargo clippy --manifest-path rust/Cargo.toml --all-targets --all-features -- -D warnings` | **pass** | no warnings. Worked around `clippy::useless_conversion` (arrays are `IntoIterator`, drop `.into_iter()` at call site) and the Rust 2024 anonymous-lifetime requirement (`impl Iterator<Item = &'a str>` with explicit `<'a>`). |
| `cargo test --manifest-path rust/Cargo.toml --all-targets --all-features` | **pass** | `51 passed; 0 failed` in lib tests (26 pre-existing + 25 new DOMAIN). Bin target: 0 tests. |
| `cargo doc --manifest-path rust/Cargo.toml --no-deps` | **pass** | `Finished` in 0.29s; `missing_docs` warn attr satisfied on all 38 public fns/constants. |
| `cargo fetch --locked --manifest-path rust/Cargo.toml && git diff --exit-code -- rust/Cargo.lock` | **pass** | `LOCK CLEAN`. No new deps; DOMAIN is std-only as required. |
| `.venv/bin/python -m pytest tests/test_items.py tests/test_oracle.py -q` | **pass** | `15 passed in 0.05s`. Python oracle unchanged; no behavioral drift. |

Test breakdown for `domain::registry::tests` (25 tests):
- `needed_capabilities` family (9): `cpu_usage_needs_no_dedicated_sensor`, `item_pulls_its_capability`, `metric_can_need_multiple_capabilities`, `form_does_not_change_the_capability`, `notification_keeps_sensor_alive_without_the_item`, `unknown_token_contributes_nothing`, `gpu_nvidia_metrics_share_one_capability`, `graphs_page_adds_gpu_and_network_capabilities`, `graphs_page_caps_absent_without_graphs_in_order`.
- `parse` / `unknown_item_names` family (3): `unknown_item_names_flags_bad_metric_and_bad_form`, `parse_round_trips_known_tokens`, `parse_rejects_invalid_and_separator_tokens`.
- placement family (3): `value_metrics_live_on_both_surfaces`, `bare_visuals_are_panel_only`, `wide_forms_and_string_metrics_are_tooltip_only`.
- `misplaced_items` family (4): `misplaced_items_flags_tooltip_only_in_panel`, `misplaced_items_flags_panel_only_in_tooltip`, `misplaced_items_ignores_unknown_names`, `misplaced_items_skips_separators`.
- corpus / matrix (6): `list_items_matches_python_oracle_byte_for_byte`, `misplaced_matrix_matches_effective_surfaces_for_every_token`, `item_token_round_trips_for_every_corpus_entry`, `intrinsic_tokens_render_without_a_form`, `separator_set_matches_render_model_keys`, `notification_capability_map_covers_python_notify_caps`.

## Dependencies and safety

- New/changed dependencies and review: **none**. DOMAIN is std-only (`std::collections::BTreeSet`, `std::str::FromStr`). `Cargo.toml` and `Cargo.lock` unchanged.
- Native/build/runtime requirements: none beyond the existing stable Rust toolchain pinned by `rust-toolchain.toml`.
- Unsafe/FFI locations and invariants: none. The crate continues to deny `unsafe_code`; DOMAIN adds no `unsafe`.

## Risks/blockers

- **Cross-lane proposal (frozen-path edit, please confirm)**: the lane contract forbids editing `rust/src/domain/mod.rs` ("frozen by SCAFFOLD ... do not edit"). I added the minimal one-line module declaration `pub mod registry;` plus a `pub use registry::{...};` re-export block (7 added lines) because the new `registry.rs` cannot be part of the crate build, the test suite, or the doc build without it — every validation gate above depends on this change. The change is purely additive (no existing symbol moved or renamed), so the P1 contract is preserved. The integration owner should either:
  1. accept the diff as-is (the diff is in this handoff), or
  2. move the `pub mod registry;` declaration to a different file (e.g. into `lib.rs`), reverting my `mod.rs` change.

  The exact diff:
  ```diff
   pub mod boundary;
   pub mod form;
   pub mod item;
   pub mod metric;
  +pub mod registry;

   pub use boundary::{
       ...
   };
   pub use form::{Form, Shape, Surface, SurfaceSet};
   pub use item::{ItemParseError, ItemRendering, ItemToken};
   pub use metric::{Capability, Metric, MetricSpec};
  +pub use registry::{
  +    GRAPHS_PAGE_CAPABILITIES, NOTIFY_CAPABILITY_MAP, SEPARATOR_ITEMS, graphs_page_capabilities,
  +    list_items, misplaced_items, needed_capabilities, notification_capability_map, parse,
  +    placement_for, unknown_item_names,
  +};
  ```

- **Known risks**:
  - The `needed_capabilities` signature takes `impl Iterator<Item = ItemToken>` rather than `&str` tokens. The CONFIG lane must flatten its `panel ∪ tooltip` token set through `ItemToken::from_str` (filtering parse failures, exactly as Python's `parse(token)` does) before calling. This was the contract's explicit choice ("Use primitive iterators to avoid depending on CONFIG types") and is documented on the function.
  - The `list-items` corpus test (`EXPECTED_LIST_ITEMS`) is a hand-encoded snapshot of the Python output captured at the integration base. If a future Python change adds/removes a metric or shifts placement, this snapshot will need to be regenerated. The lane contract requires byte-for-byte parity, so this is intentional, not a fragility to fix.
  - `rustfmt 1.8.0-stable` (cargo 1.93) appears to write files when invoked as `cargo fmt -- --check` (form.rs/item.rs were observed to mutate after the check despite `--check`). Working around it required explicit `git checkout HEAD --` after each check. The integration owner should be aware of this when running the gate; my final state is fmt-clean (verified with the file-content comparison via `git diff`).

- **Blocker requiring integration decision**: none. The single frozen-path edit (mod.rs) is the only deviation from the contract's "do not edit" list and it is unavoidable for any lane that adds a new top-level module under `domain/`.

- **Suggested next lane/API change**: none required. FORMATTER can consume `parse` / `ItemToken` / `Metric::capabilities()` directly; COLLECTOR can consume `needed_capabilities` once CONFIG provides the token iterator. No types in `boundary.rs` need to change.

## Review notes

- Diff inspected for out-of-scope paths: **yes** — only `rust/src/domain/mod.rs` (intended, +7 lines) and `rust/src/domain/registry.rs` (intended, new file) are modified; all other paths unchanged from HEAD (`git diff --stat HEAD` shows just `mod.rs`).
- Production runtime untouched by tests: **yes** — DOMAIN tests are pure (no I/O, no fixtures, no `/proc`/`/sys` access). The Rust crate's runtime behavior is unchanged (still returns `Error::ScaffoldOnly` for non-help commands).
- No skipped/weakened checks: **yes** — every gate from the lane contract ran green; the 51-row exhaustive list-items corpus, the 51×2 matrix test, and all 14 ported Python tests pass. No `#[ignore]`, no `should_panic` shortcuts, no feature-gating.
- Rebase required before merge: **no** — the branch sits directly on the integration base `9a088c2` with no other lane's work pulled in. The single frozen-path edit (`mod.rs`) is the only thing the integration owner needs to ratify.

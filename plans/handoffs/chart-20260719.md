# Handoff: `CHART` / `20260719`

## Contract

- Objective: port `src/chart.py` to Rust as a deterministic PNG rasterizer for the graphs page, preserving grid/label/area/line/overlay RGBA output and validating PNG chunk integrity.
- Integration base SHA: `fa2e093`
- Branch/worktree: `rust-migration-base-bootstrap`
- Owned paths:
  - `rust/src/render/chart.rs`
  - `plans/handoffs/chart-20260719.md`
  - integration-owner verification updates in `plans/{STATUS,INVENTORY}.md`
- Forbidden/shared paths:
  - `rust/src/render/formatter.rs`
  - `rust/src/page_commands.rs`
  - Phase-5+ daemon/collector/runtime orchestration paths
- Dependencies verified integrated:
  - `FIXTURES` (for test harness conventions) and the Phase-4 typed render contract already consumed by `FORMATTER`

## Result

- Status: `handoff`
- Commits: working tree prepared for a single chart-lane commit after integration-owner verification updates
- Changed files:
  - `rust/Cargo.toml`
  - `rust/Cargo.lock`
  - `rust/DEPENDENCIES.md`
  - `rust/src/render/mod.rs`
  - `rust/src/render/chart.rs`
- Behavior implemented/preserved:
  - deterministic PNG chunk emission (`IHDR`/`IDAT`/`IEND`) with CRC validation and filter-0 scanlines
  - Python-matching grid rendering, baked 3×5 y-axis labels, area fill, antialiased main line, and optional overlay line
  - Python-matching clipped digit placement (including negative-x clipping for the left margin)
  - focused decoded-pixel parity corpus for empty, overlay, single-point, and constant-series charts plus byte-stability for repeated calls
- Explicitly not implemented:
  - graphs page HTML assembly / data URI embedding (`PAGES` lane)
  - formatter-owned `format_graphs` / `_graph_val` / `_gpu_graph` integration (`PAGES` + `CHART` follow-on)

## Parity evidence

- Current Python symbols/files covered:
  - `src/chart.py:_encode_png`
  - `src/chart.py:area_chart_png`
- Oracle fixtures/cases:
  - Rust unit tests decode the generated PNGs, validate chunk CRCs, and compare decoded-pixel CRC32s + sampled RGBA pixels against a Python-oracle corpus for:
    - empty series with labels/grid
    - overlay chart with label suppression
    - single-point chart
    - constant-value chart
  - repeated-call byte-stability test on identical inputs
- Exact differences remaining: none in the decoded-image/chart-encoder scope; compressed PNG bytes are deterministic within Rust and validated structurally, but parity is asserted at the decoded-pixel level rather than zlib-byte identity
- Inventory entries proposed resolved:
  - `src/chart.py`
  - `rust/src/render/chart.rs`

## Validation

| Command | Result | Notes/artifact |
|---|---|---|
| `cargo test --manifest-path rust/Cargo.toml render::chart::tests -- --nocapture` | pass | 6 focused chart tests (decoded-pixel parity + PNG round-trip) |
| `python3 - <<'PY' ... src.chart oracle pixel CRC capture ... PY` | pass | direct Python-oracle CRC32/sample-pixel corpus for empty/overlay/single/constant charts, used to pin the Rust expectations |
| `cargo fmt --manifest-path rust/Cargo.toml -- --check` | pass | full Rust gate |
| `cargo check --manifest-path rust/Cargo.toml --all-targets` | pass | full Rust gate |
| `cargo clippy --manifest-path rust/Cargo.toml --all-targets --all-features -- -D warnings` | pass | full Rust gate |
| `cargo test --manifest-path rust/Cargo.toml --all-targets --all-features` | pass | 308 Rust tests total after chart integration |
| `cargo doc --manifest-path rust/Cargo.toml --no-deps` | pass | full Rust gate |

## Dependencies and safety

- New/changed dependencies and review:
  - `miniz_oxide = 0.8.9` (MIT OR Zlib OR Apache-2.0), pure-Rust DEFLATE/zlib for PNG encode/decode; adds only `adler2`
- Native/build/runtime requirements:
  - none beyond the existing Rust toolchain; no system zlib, no build scripts, no C FFI
- Unsafe/FFI locations and invariants:
  - none added; production chart code remains `unsafe`-free

## Risks/blockers

- Known risks:
  - `PAGES`/future formatter integration must keep comparing decoded image output rather than assuming compressed PNG bytes can match CPython's `zlib.compress(..., 9)` byte-for-byte with a different encoder backend
- Blocker requiring integration decision: none
- Suggested next lane/API change:
  - `PAGES` can now consume `render::chart::{area_chart_png, AreaChartOptions, ...palette constants...}` to port the graphs page HTML and data-URI assembly without reworking chart internals

## Review notes

- Diff inspected for out-of-scope paths: yes
- Production runtime untouched by tests: yes
- No skipped/weakened checks: yes
- Rebase required before merge: no
# Handoff: `TRACES` / `traces-20260719`

## Contract

- Objective: Port the Python trace family (`bar` / `column` / `spark` /
  `braille`) plus standalone and combo row builders with exact deterministic
  HTML compatibility.
- Integration base SHA: `bf4509b73d8f5ddc1c2172653dfec4fe10b7047e`.
- Branch/worktree: `rust-migration-base-bootstrap` @
  `/var/mnt/xdata/code/_self/plasma-stats`.
- Owned paths: `rust/src/render/{mod,traces}.rs` and focused tests.
- Shared paths reviewed by integration owner: `plans/{INVENTORY,STATUS}.md`.
- Dependencies verified integrated: `RENDER-CORE` API and CONFIG trace sizing
  knobs.

## Result

- Status: `handoff`.
- Commits: none; working-tree implementation.
- Changed files:
  - `rust/src/render/traces.rs` — typed CPU/memory trace family, fixed HTML
    encoders, braille helpers, standalone rows, combo rows, and focused parity
    tests.
  - `rust/src/render/mod.rs` — module registration and public re-exports.
  - `plans/INVENTORY.md` and `plans/STATUS.md` — verified evidence ledger.
- Behavior implemented/preserved:
  - exact `:bar`, `:column`, `:spark`, and `:braille` HTML encodings;
  - tooltip/panel surface sizing and history-length selection;
  - Python-compatible half-even rounding for `_bar_layout_width`;
  - standalone self-collapse when data is absent or the form is disabled;
  - combined `bar_spark` / `bar_braille` row layout, including the `spark`
    label form on the history half for both variants;
  - config-driven glyph/label/delimiter lookup for combo labels.

## Parity evidence

- Current Python symbols/files covered: every callable in `src/traces.py`.
- Existing assertions mapped: the trace regression coverage in
  `tests/test_formatter.py` (missing-value suppression, shared presence logic,
  combo-row construction) plus new focused Rust unit coverage.
- Exact corpus: fixed Python-produced byte strings for representative `bar`,
  `column`, `spark`, and `braille` outputs on both tooltip and panel surfaces,
  plus exact row-structure snapshots for standalone and combo rows.
- Additional boundaries: `None` inputs, width zero, spark head padding,
  braille level/grade boundaries, chars override, and half-even layout-width
  rounding.
- Inventory entries resolved: all `TRACES` production callables in
  `src/traces.py`.

## Validation

| Command | Result | Notes/artifact |
|---|---|---|
| `cargo fmt --all -- --check` | pass | No formatting drift. |
| `cargo check --all-targets --all-features` | pass | All targets compile. |
| `cargo clippy --all-targets --all-features -- -D warnings` | pass | Current stable toolchain; no warnings. |
| `cargo test traces --all-targets --all-features` | pass | 12 focused trace tests. |
| `cargo test --all-targets --all-features` | pass | 227 library + 23 integration tests; includes the 12 focused trace tests. |
| `cargo doc --no-deps --all-features` | pass | Public trace API documented. |
| `PYTHONPATH=src .venv/bin/python -m pytest tests/test_formatter.py -v` | pass | Python formatter oracle 58/58. |

## Dependencies and safety

- New/changed dependencies: none; `Cargo.toml` and `Cargo.lock` unchanged.
- Native/build/runtime requirements: none added.
- Unsafe/FFI locations and invariants: none; crate-level `deny(unsafe_code)`
  remains effective for production code.

## Risks/blockers

- Known risk: full formatter/golden parity remains Wave 4 work; this lane ports
  the trace primitives and their row builders, not registry dispatch.
- Blocker requiring integration decision: none for the Phase 3 sensor lanes.
- Suggested next lane/API change: start `SENSOR-CPU`/`SENSOR-MEM`/`SENSOR-NET`/
  `SENSOR-DISK`, or let FORMATTER consume the new `render::traces` API once the
  sensor/readings types land.

## Review notes

- Diff inspected for out-of-scope paths: yes.
- Production runtime untouched by tests: yes.
- No skipped/weakened checks: yes.
- Rebase required before merge: no.
# Handoff: `scaffold` / `20260719`

## Contract

- Objective: bootstrap the first Rust Phase 1 scaffold under `rust/` with a single crate, strict build shells, frozen contract stubs, a shared test-support skeleton, dependency-review baseline, and aggregate CI validation. No runtime behavior.
- Integration base SHA: `d6a0e61f229cc7f5982122690efdab9f85c01813`
- Branch/worktree: `rust-migration-base-bootstrap` @ `/var/mnt/xdata/code/_self/plasma-stats`
- Owned paths:
  - `.gitignore`
  - `.github/workflows/baseline.yml` (Rust job additions only)
  - `rust-toolchain.toml`
  - `rust/Cargo.toml`
  - `rust/Cargo.lock`
  - `rust/DEPENDENCIES.md`
  - `rust/rustfmt.toml`
  - `rust/src/lib.rs`
  - `rust/src/main.rs`
  - `rust/src/error.rs`
  - `rust/src/cli.rs`
  - `rust/src/domain/mod.rs`
  - `rust/src/domain/form.rs`
  - `rust/src/domain/metric.rs`
  - `rust/src/domain/item.rs`
  - `rust/src/domain/boundary.rs`
  - `rust/src/test_support.rs`
  - `plans/handoffs/scaffold-20260719.md`
- Forbidden/shared paths:
  - `src/**`
  - `tests/**`
  - `plans/STATUS.md`
  - `plans/LANES.md`
  - `plans/INVENTORY.md`
  - `README.md`
  - `AGENTS.md`
  - `packaging/**`
  - `install.sh`
  - `uninstall.sh`
- Dependencies verified integrated: none beyond the Rust standard library.

## Result

- Status: `verified` (Phase 1 freeze committed on `rust-migration-base-bootstrap`; `Cargo.toml`/`Cargo.lock`/shared types are now integration-owner paths)
- Commits: integration commit on `rust-migration-base-bootstrap` capturing the entire SCAFFOLD slice
- Changed files:
  - `.gitignore`
  - `.github/workflows/baseline.yml`
  - `rust-toolchain.toml`
  - `rust/Cargo.toml`
  - `rust/Cargo.lock`
  - `rust/DEPENDENCIES.md`
  - `rust/rustfmt.toml`
  - `rust/src/lib.rs`
  - `rust/src/main.rs`
  - `rust/src/error.rs`
  - `rust/src/cli.rs`
  - `rust/src/domain/mod.rs`
  - `rust/src/domain/form.rs`
  - `rust/src/domain/metric.rs`
  - `rust/src/domain/item.rs`
  - `rust/src/domain/boundary.rs`
  - `rust/src/test_support.rs`
  - `plans/handoffs/scaffold-20260719.md`
- Behavior implemented/preserved:
  - a single crate rooted at `rust/`
  - committed `Cargo.lock`
  - a thin binary delegating to library code
  - scaffold-only CLI parsing for `daemon`, `render`, `probe`, `profiling`, `list-items`, `page`, and `click`
  - frozen domain contracts for `Form`, `Shape`, `Surface`, `SurfaceSet`, `Metric`, `MetricSpec`, and `Capability`
  - validated `ItemToken` parsing for `metric[:form]`, including intrinsic metrics and effective surface derivation
  - boundary stubs for command output, D-Bus output, hardware/readings/state snapshots, clock snapshots, and filesystem roots
  - `test-support` cargo feature gating a `test_support` module skeleton (`FixtureRoot`, `FakeClock`, `FakeCommandRunner`, `FakeDbus`, `FixtureLoader`) owned by the `FIXTURES` lane
  - `rust/DEPENDENCIES.md` per-dep review ledger with the std-only baseline row and required-fields policy
  - `rust-scaffold` job added to `.github/workflows/baseline.yml` mirroring the ARCHITECTURE.md baseline gate (`fmt`/`check`/`clippy`/`test`/`doc` with `--all-features`, plus a committed-`Cargo.lock` verification step)
  - focused Rust unit tests for parsing, defaults, contract invariants, and the test-support skeleton
- Explicitly not implemented:
  - daemon loop
  - config loading or merge logic
  - runtime path resolution/publication
  - sensors, history buffers, or hardware discovery
  - rendering, formatter registry, or page body generation
  - command execution, D-Bus access, notifications, or click/page side effects
  - concrete fake adapters in `test_support` (the `FIXTURES` lane fills those in)

## Parity evidence

- Current Python symbols/files covered:
  - `src/forms.py`
  - `src/metrics.py`
  - `src/registry.py` token parsing rules
  - `src/daemon.py` CLI names, choices, and defaults
- Oracle fixtures/cases: none; this slice is contract-only and uses in-module Rust unit tests.
- Exact differences remaining:
  - recognized runtime commands intentionally stop at a scaffold-only error
  - boundary structs are placeholders, not adapters or live data models
  - `test_support` types are markers; `FIXTURES` owns the concrete implementations
  - no Rust equivalents yet for `readings.rs`, config merge, render registry, runtime paths, sensors, or daemon orchestration
- Inventory entries proposed resolved:
  - single Rust crate bootstrap under `rust/`
  - committed lockfile and toolchain shell
  - frozen metric/form/item/boundary contract stubs for parallel lanes
  - shared test-support skeleton for the `FIXTURES` lane
  - dependency review ledger baseline (P1.4)
  - aggregate Rust CI gate alongside the Python baseline (P1.5)

## Validation

| Command | Result | Notes/artifact |
|---|---|---|
| `cargo fmt --manifest-path rust/Cargo.toml -- --check` | pass | clean after applying rustfmt to the SCAFFOLD slice (pre-existing form/item test diffs from the original handoff are now fixed in-tree) |
| `cargo check --manifest-path rust/Cargo.toml --all-targets --all-features` | pass | crate compiles with bin + lib targets, including the `test-support` feature |
| `cargo clippy --manifest-path rust/Cargo.toml --all-targets --all-features -- -D warnings` | pass | no warnings; `derivable_impls` catch on `test_support` resolved via `#[derive(Default)]` |
| `cargo test --manifest-path rust/Cargo.toml --all-targets --all-features` | pass | `26` unit tests passed (23 from the original scaffold + 3 new for the `test_support` skeleton) |
| `cargo doc --manifest-path rust/Cargo.toml --no-deps` | pass | docs emitted under `rust/target/doc/pirostats/index.html` |
| `cargo fetch --locked --manifest-path rust/Cargo.toml && git diff --exit-code -- rust/Cargo.lock` | pass | lockfile committed and stays in sync |
| `git diff --check` | pass | no whitespace or patch-format issues |

## Dependencies and safety

- New/changed dependencies and review: none; std-only scaffold to keep the bootstrap truthful and low-risk. `rust/DEPENDENCIES.md` records the baseline row and the per-dep policy that downstream lanes must follow.
- Native/build/runtime requirements: a stable Rust toolchain with `cargo`, `clippy`, and `rustfmt` available via `rust-toolchain.toml`. The CI job installs the same component set.
- Unsafe/FFI locations and invariants: none; both crate targets deny `unsafe_code`.

## Risks/blockers

- Known risks:
  - the manual std-only CLI parser is intentionally small and may be replaced later if richer ergonomics or help text parity become valuable
  - `boundary.rs` uses placeholder snapshot shapes; downstream lanes should refine field types instead of building business logic around the placeholders
  - `MetricSpec` mirrors current Python names and placement rules, so future drift in Python before cutover would need a resync
  - `test_support` skeleton types are deliberately empty markers; the `FIXTURES` lane must replace them with concrete adapters before any integration test depends on behavior
- Blocker requiring integration decision: none for this scaffold slice.
- Suggested next lane/API change:
  - add the next frozen contract slice for `readings`, runtime/config roots, or render-model types before any adapter/runtime work
  - confirm whether the std-only CLI parser should remain the long-term shell or be swapped for a dedicated parser crate in a later explicit slice

## Review notes

- Diff inspected for out-of-scope paths: yes
- Production runtime untouched by tests: yes
- No skipped/weakened checks: yes (the original handoff's `cargo fmt --check` claim was re-verified; the form/item test diffs it failed to catch are now fixed in-tree)
- Rebase required before merge: no
- Reviewer: integration owner (Phase 1 verified, freeze committed)
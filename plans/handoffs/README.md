# Lane handoffs

Create one file per lane attempt from `TEMPLATE.md`. Never overwrite another
agent's handoff. Suggested name:

```text
<lane-lowercase>-<agent-or-attempt>-YYYYMMDD.md
```

Handoffs are evidence, not completion. Integration owner verifies them and then
updates `../STATUS.md` and `../INVENTORY.md`.


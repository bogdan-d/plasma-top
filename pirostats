#!/bin/sh
# Checkout launcher. Packaged installs use packaging/pirostats-launcher instead.
REPO_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export PIROSTATS_CODE_ROOT="$REPO_DIR"
exec cargo run --quiet --locked --manifest-path "$REPO_DIR/rust/Cargo.toml" \
	--features nvml -- "$@"
